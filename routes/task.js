// const router = require("express").Router();
// const tasksController = require("../../controllers/tasksController");

// // Matches with "/api/tasks"
// router
//   .route("/")
//   .get(tasksController.findAll) // get all chores belonging to user's household
//   .post(tasksController.create) // create a task
//   .put(tasksController.update) //update a task


// // Matches with "/api/tasks/deactivate"
// router
//   .route("/deactivate")
//   .post(tasksController.deactivate); // deactivate a task

// module.exports = router;